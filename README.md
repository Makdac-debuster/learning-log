# learning-log
Заметки об обучении программированию
# Learning log

Дневник обучения: Python, Git, Linux.

## 2026-09-15
- Настроил Git, Python 3.14, VS Code, SSH-ключ наверное 
- Создал первый репозиторий ы
## Неделя 2 — терминал

Разобрал: pwd, ls, cd, mkdir, cat, cp, mv, rm.
Пути: абсолютные и относительные, `.` и `..`.
Автодополнение по Tab, история команд стрелкой вверх.

Ошибки, на которых учился:
- GH007 при push — Git подписывал коммиты личной почтой при включённой приватности.
  Починил: noreply-адрес в конфиге + git commit --amend --reset-author.
- Ввёл путь без команды — терминал ищет программу с таким именем. Пути нужен глагол
## Первый код на Python

Запустил hello.py: input(), print(), f-строки.
input() всегда возвращает строку — для арифметики нужен int().
Разобрал структуру трассировки ошибки: файл, строка, фрагмент, тип ошибки.